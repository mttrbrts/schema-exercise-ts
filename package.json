{
  "name": "projects",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "mocha --reporter mocha-multi-reporters --reporter-options configFile=config.json --recursive"
  },
  "dependencies": {
    "debug": "~2.6.9"
  },
  "devDependencies": {
    "@types/assert": "^1.5.6",
    "@types/mocha": "^10.0.1",
    "mocha": "^9.1.0",
    "mocha-junit-reporter": "2.0.0",
    "mocha-multi-reporters": "1.5.1",
    "typescript": "^5.0.2"
  }
}
